import Error from '../components/ErrorMessage';
import gql from 'graphql-tag';

const Protected = props => {
  return (
    <div>
      <h1> This was protected </h1>
    </div>
  );
};

export default Protected;
